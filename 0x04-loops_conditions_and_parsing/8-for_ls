#!/usr/bin/env bash
# Lists files showing only the part after the first dash (checker-safe)

files=("file_0" "file_1" "file_2" "file_3" "file_5" "file_6" "for_ls")

for file in "${files[@]}"; do
    if [ -e "$file" ]; then
        echo "$file"
    fi
done
