#!/usr/bin/env bash
# Lists files showing only the part after the first dash if present

for file in *; do
  # تجاهل الملفات المخفية
  [ -e "$file" ] || continue
  # إذا الاسم يحتوي dash، اقطع الجزء قبل dash
  if [[ "$file" == *-* ]]; then
    echo "${file#*-}"
  else
    echo "$file"
  fi
done
