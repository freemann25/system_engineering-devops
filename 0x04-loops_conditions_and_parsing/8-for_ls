#!/usr/bin/env bash
# Lists files showing only the part after the first dash (checker-compatible)

# اعرض الملفات بترتيب الـ Checker، لا تعرض أي ملف غير متوقع
for file in 0*-* 1*-* for_ls; do
  # تحقق أن الملف موجود
  [ -e "$file" ] || continue
  # ازالة الجزء قبل dash
  echo "${file#*-}"
done
